-- ============================================
-- System Backlog Table — DEPRECATED
-- ============================================
-- This file is superseded by system_backlog_schema.sql
-- which uses INTEGER priority (1-10) and PENDING/COMPLETED status.
-- Kept for reference only. Do NOT deploy this version.
--
-- The canonical schema is: system_backlog_schema.sql
-- ============================================

-- Original (deprecated) schema:
-- CREATE TABLE IF NOT EXISTS system_backlog (
--     id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--     title TEXT NOT NULL,
--     description TEXT,
--     priority TEXT NOT NULL, -- HIGH, MID, LOW (old format)
--     status TEXT NOT NULL DEFAULT 'NEW', -- NEW, IN_PROGRESS, DONE (old format)
--     category TEXT DEFAULT 'FEATURE',
--     created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
--     updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
-- );
