# Python cache
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual environments
venv/
env/
ENV/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo

# Environment files (CRITICAL - Never commit these!)
.env
.env.local
.env.adk
*.env

# Logs
*.log
heartbeat.log
heartbeat_adk.log
founder_notifications.log

# Data & State
dashboard.json
reasoning_trace.json
*.backup

# OS
.DS_Store
Thumbs.db

# Dependencies
node_modules/

# Credentials
*.pem
*.key
credentials.json
google-credentials.json
github-ci-key.json
new_api_key.txt
*.txt.key

# Claude local memory/session (not source) — behalve de project-skills, die horen
# bij de repo. settings.local.json, hooks, worktrees en locks blijven lokaal.
.claude/*
!.claude/commands/
.claude/commands/*
!.claude/commands/*.md

# Junk / OS artifacts
nul
logs.txt
imports_log.txt
models.txt
pro_models.json
dummy.txt

# Remote state snapshots (pulled from VM for inspection)
remote_dashboard.json
remote_trade_log.json

# Runtime state (VM-owned, not source)
trade_log.json
active_assets.json
ticker_state.json
pipeline_events.json
decision_history.json
cpo_state.json
pl_status.json
pl_meta.json
data_cache.json
learning_report.json
swarm_health.json
supabase_health.json
audit_log.txt
curl_out.txt
check.html
swarm_vm.log
# docker-compose.prod.yml is committed — it has no secrets (refs .env.adk on VM)

# Runtime state not already listed above
pnl_snapshots.json
swarm_health.json

# Dev one-off artifacts
*.ps1.bak
curl_out.txt
new_api_key.txt

# --- Repo hygiene (added 2026-06-01) ---
# Base64 transfer artifacts from hot-patch deploys
_b64_*.txt
*_b64.txt
# Local snapshots pulled from the VM for inspection
*_live.json
*_audit.json
*_check.json
trade_log_*.json
decision_history_check.json
scripts/replay_geometry_results.json
# Ad-hoc local debug scripts (root-level and underscore-prefixed)
/_*.py
scripts/_*.py
# Stocks pipeline runtime state (VM-owned, not source)
stocks_active_positions.json
stocks_decision_history.json
stocks_pending_approval.json
stocks_trade_log.json
stocks_watchlist.json

# --- Repo hygiene (opschoning 2026-07-06) ---
# Archief van one-off scratch/diagnose-bestanden (niet in source, niet in builds)
_archive/
_audit/
# Codex CLI lokale config
.codex/
# Runtime state niet eerder gedekt
polymarket_shadow_log.json
shadow_book.json
shadow_report.json
treasury_state.json
treasury_harvest.json
treasury_proposals.json
market_regime.json
cost_log.json
portfolio_peak.json
audited_trades.json

# Conviction Barbell (persoonlijke portefeuille-waardes / rebalance-state)
barbell_holdings.json
barbell_rebalance_log.json

# Playwright MCP scratch-snapshots
.playwright-mcp/

# Gegenereerd door research/track.py dashboard (tracking.json is wel de historie)
research/dashboard.html

# Wekelijkse screen-uitvoer (de melding en de kaarten zijn het resultaat)
research/screen_result.json
