# Versies gepind op wat de productiecontainer daadwerkelijk draait
# (`docker exec agent_trader_swarm pip freeze`, gemeten 2026-08-11).
#
# Waarom exact (`==`) en niet `>=`:
#   1. Zonder ondergrenzen kon pip 26 de graaf niet meer oplossen —
#      `error: resolution-too-deep`. Dat brak zowel ci.yml als deploy.yml,
#      dus er kon niet meer gedeployd worden.
#   2. Dit project heeft een gedocumenteerde geschiedenis van stille breuk door
#      versie-drift (o.a. het uitfaseren van het Gemini fast-model, dat elke
#      thinking=False-aanroep swarm-breed nekte). De container die je bouwt hoort
#      de container te zijn waarvan bewezen is dat hij werkt.
#
# Bij het bijwerken van een pakket: verhoog hier, deploy, en controleer met
# `python -m tests.pre_flight.check_pipeline` voordat je het commit.

# Core AI & Agent Framework
google-cloud-aiplatform==1.163.0
google-generativeai==0.8.6

# GCP Cloud Integration
google-cloud-secret-manager==2.29.0
google-cloud-logging==3.16.0

# Data Processing
pandas==3.0.5
numpy==2.4.6

# Trading Infrastructure
ccxt==4.5.56
eth-account==0.13.7

# Database & State Management
supabase==2.31.0

# Utilities
python-dotenv==1.2.2
pydantic==2.13.4
aiohttp==3.14.3
nest-asyncio==1.6.0

# Web Intelligence
beautifulsoup4==4.15.0
requests==2.34.2
duckduckgo-search==8.1.1

# Stocks Department (Phase 1)
yfinance==1.5.2
ib_insync==0.9.86

# Automation & Testing
playwright==1.61.0
pytest-playwright==0.8.0
